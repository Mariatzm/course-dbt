version: 2

models:
  - name: int__events_info
    description: "Events information"
    columns:
          - name: EVENT_ID
            description: "The unique event id"
            tests:
              - unique
              - not_null